<Grid xmlns="root" Background="GhostWhite" xmlns:local="using:Common;assembly=Common">

    <Button Grid.Row="1" HorizontalAlignment="Right" Margin="8" Command="{Bind ShowMessageCommand}" RequestedSize="100,-">
        <Button.Content>
            <TextBlock Text="Hola"></TextBlock>
        </Button.Content>
    </Button>

</Grid>