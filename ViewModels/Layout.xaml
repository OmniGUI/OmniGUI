<Grid xmlns="root" Background="GhostWhite" xmlns:local="using:Common;assembly=Common">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <List Source="{Bind People}" />
        <Button Grid.Column="1" Content="Añadir" VerticalAlignment="Top" Command="{Bind DeleteItemCommand}" />
    </Grid>
    
</Grid>